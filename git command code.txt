Delete git History:
rm -rf .git

Reinit:
git init
git add .
git commit -m "Initial commit"

Remote & Push:
git remote add origin https://github.com/brendan-okeyiwobi/lytude-web.git
git remote add origin https://github.com/brendan-okeyiwobi/soundlytude-web.git
git branch -M main
git push -u origin main (git push -u origin main --force)

Pull and Merge:
git pull origin main --allow-unrelated-histories


Making a change:
# Step 1: Make your change locally
git add .
git commit -m "Fix SEO metadata and description"

# Step 2: Push to GitHub
git push origin main
